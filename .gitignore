# Lark CLI config (contains credentials)
.lark-cli/

# Local tool-specific adapters. The canonical skill source stays in
# .agents/skills/ and these directories should only be generated locally
# when a tool cannot read the repository layout directly.
.claude/
.kiro/
.codex/

# Temporary files
*.tmp
*.log
.DS_Store
